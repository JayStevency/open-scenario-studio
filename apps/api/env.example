# apps/api/.env 로 복사해 쓴다. 없어도 저장소 안 data/scenario-studio.db 로 붙는다.
# 다른 위치를 쓸 때만 지정한다. 반드시 절대 경로로.
# DATABASE_URL="file:/Users/me/somewhere/scenario-studio.db"

PORT=3000
WEB_ORIGIN="http://localhost:5173"

# 이력에 남을 에이전트 이름 (MCP)
OSS_AGENT_LABEL="mcp-agent"
OSS_PROJECT_ID="default"
