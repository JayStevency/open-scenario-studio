node_modules/
dist/
coverage/
*.tsbuildinfo

# Prisma 생성물 — pnpm db:generate 로 만든다
apps/api/src/generated/

.DS_Store
.omc/
.env
.env.local

# SQLite 데이터 파일 — pnpm db:migrate && pnpm db:seed 로 만든다
data/
