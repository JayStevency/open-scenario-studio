node_modules/
dist/
coverage/
*.tsbuildinfo

# Prisma 생성물 — pnpm db:generate 로 만든다
apps/api/src/generated/

.DS_Store
.omc/
.env
.env.local

# SQLite 데이터 파일 — pnpm db:migrate && pnpm db:seed 로 만든다
data/

# 실제 시나리오 데이터 — 프로젝트마다 다르고 내부 명세라 올리지 않는다
design/data/
